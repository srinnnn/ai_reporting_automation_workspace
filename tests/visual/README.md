# Visual Regression Foundation

This directory establishes the Middle Platform visual regression foundation.

Current status:

- `STRUCTURAL_VISUAL_REGRESSION = READY`
- `VISUAL_REGRESSION = PARTIAL`
- `RUNTIME_VISUAL_BASELINE = PENDING_SCREENSHOT_COMPARISON`

## Baseline Routes

- `/` -> `tests/visual/baseline/homepage.png`
- `/` -> `tests/visual/baseline/homepage-structure.json`
- `/workspace/ANTA` -> `BASELINE_PENDING`
- `/workspace/ANTA/P1` -> `BASELINE_PENDING`
- `/schedule` -> `BASELINE_PENDING`

## Source Layers

- Product design source: `docs/ui-reference/homepage-approved.png`
- Runtime screenshot evidence: `tests/visual/runtime/homepage.png`
- Operational structural baseline: `tests/visual/baseline/homepage-structure.json`

The runtime homepage screenshot is generated by Playwright from `http://127.0.0.1:8785/`.

## Gate

Playwright compares the homepage runtime structure against `homepage-structure.json`. Unapproved changes to global filter order, brand selector semantics, selected-brand banner count, flat brand-card grid usage, brand-scoped KPI/project behavior, or P1-P4 category placement fail CI.

Pixel screenshot evidence remains captured at `tests/visual/runtime/homepage.png`; screenshot diff comparison against a confirmed runtime baseline is still pending and must not be reported as a full operational visual regression gate.
