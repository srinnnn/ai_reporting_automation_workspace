# 项目知识库

更新时间：2026-07-30
项目目录：`C:\Users\JM042403\Documents\ai_reporting_automation_workspace`

## 1. 项目目标

本项目是一个企业内部业务自动化工作台，目标是把组内重复性高、规则明确、可通过数据驱动判断的工作沉淀为统一系统能力。

- P1 数据处理提效：短彩信数据处理、日报、周报、月报、指标分析。
- P2 内容生产提效：AI 选品、人群策略、卖点提炼、文案生成、视觉 Brief、质检结果。
- P3 配置自动化提效：安踏即时零售上下架筛选、素材筛选、黑名单筛选等配置类流程。
- P4 储备规划：后续品牌、渠道、配置场景和自动化能力扩展。
- 建立统一基础数据层：所有正式报表和 AI 结果必须从清洗后的基础数据层读取，禁止直接读取原始下载文件生成正式交付。
- 支持本地/内网试运行：在未完成公司安全审批前，系统以本地工作台和受控文件流转为主，不私自对外部署。

## 2. 当前架构

当前架构是本地内网工作台 + 浏览器下载助手 + SQLite 本地基础数据层 + Python 数据处理模块。

- `intranet_app/`：内网工作台主程序，包含页面、登录、数据入库、报表生成、P2 内容中心、自动化任务。
- `intranet_app/processors/`：具体业务处理器，包括博西短彩信、安踏日报/周报/月报、安踏上下架/黑名单、AI 选品、文案辅助。
- `browser_extensions/`：浏览器插件/下载助手，用于辅助从平台后台导出或同步数据文件。
- `tools/`：命令行工具，例如批量生成安踏美团日报、插件到日报端到端测试、百炼 API Key 配置。
- `tests/`：单元测试和页面/流程测试。
- `ai_report_config_materials/`：业务资料包目录，只允许上传模板、说明、规则文档；真实业务数据必须留在本地并被 Git 忽略。
- `data/examples/`：脱敏示例数据。
- `data/templates/`：可公开的模板文件。
- `data/local/`：本地真实业务数据目录，不上传 GitHub。

运行入口：

```powershell
python -m intranet_app.app
```

默认访问地址：

```text
http://127.0.0.1:8785
```

## 3. 模块说明

### 登录与工作台

对应代码：`intranet_app/app.py`、`intranet_app/auth.py`、`intranet_app/storage.py`

- 提供本地网页入口。
- 支持登录、会话管理、首页数据看板、P1-P4 分级入口。
- 项目开发阶段、业务反馈、后续迭代需求等信息写入本地 SQLite。

### 数据入库中心

对应代码：`intranet_app/data_foundation.py`、`intranet_app/storage.py`

- 识别上传或插件同步的文件类型。
- 执行字段映射、必填字段校验、数值格式校验、品牌归属判断。
- 校验通过后写入统一基础数据表。
- 校验不通过时只记录问题，不允许进入正式报表或 AI 输出。

### P1 报表中心

对应代码：`intranet_app/processors/anta_meituan_reporting.py`、`intranet_app/processors/anta_reporting.py`、`intranet_app/processors/bosch_sms.py`

- 博西短彩信数据处理。
- 安踏美团日报、周报。
- 安踏历史周报、月报处理逻辑。
- 日报支持按业务选择日期生成。
- 周报支持基于周维度数据生成 TOP 门店、TOP 商品和下周选品方向。

### P2 内容生产中心

对应代码：`intranet_app/content_pipeline.py`、`intranet_app/ai_gateway.py`、`intranet_app/processors/ai_selection.py`、`intranet_app/processors/copy_content.py`

- 从基础数据层读取商品销售数据和评价数据。
- 选出候选商品。
- 通过 AI 接口生成选品理由、人群场景、卖点、文案、视觉 Brief。
- 对禁用词、缺失事实、活动信息不完整等进行质检提示。
- 当前 AI 网关默认接入阿里百炼兼容 OpenAI 格式接口，模型默认 `qwen-plus`。

### P3 配置自动化

对应代码：`intranet_app/processors/anta_listing.py`、`intranet_app/processors/anta_blacklist.py`

- 安踏即时零售上下架筛选。
- 素材筛选。
- 黑名单筛选。
- 当前业务归类为配置自动化，优先级为 P3。

### 自动化执行中心

对应代码：`intranet_app/app.py`、`intranet_app/storage.py`、`tools/meituan_download_assistant_sync.py`

- 管理每日数据抓取/同步任务。
- 检查基础数据层是否已有对应日期、品牌、渠道的数据。
- 如果本地基础层缺失，需要通过插件或业务导出补齐原始文件，再进入入库流程。

### 浏览器插件/下载助手

对应目录：`browser_extensions/`

- 用于辅助美团等后台数据导出。
- 依赖业务用户已登录的平台账号。
- 不绕过验证码、MFA、平台权限限制。
- 禁止执行后台配置修改，只做浏览、导出和同步。

## 4. 数据流

正式规则：P1/P2 不允许直接读取原始文件生成正式结果。

```text
业务后台 / 浏览器插件 / 人工上传
        ↓
原始文件进入 intake 或 upload 暂存区
        ↓
文件识别：平台、品牌、渠道、文件类型、日期范围
        ↓
字段映射：平台字段 → 统一字段
        ↓
数据清洗：空值、重复、金额、日期、订单状态、口径转换
        ↓
品牌归属校验：brand_id + platform + channel + 门店/商品线索
        ↓
统一基础数据层
        ├─ fact_order_product
        ├─ fact_store_finance
        ├─ fact_store_traffic
        ├─ fact_service_review
        ├─ dim_product
        ├─ dim_store
        ├─ target_plan
        ├─ dim_campaign
        ├─ dim_platform_shop
        └─ dim_channel_product
        ↓
P1 报表 / P2 AI 内容 / P3 配置处理
        ↓
结果 CSV / 页面预览 / 业务交付
```

当前重点数据表：

- `fact_order_product`：商品订单明细，用于销售额、销量、订单数、商品 TOP、选品候选。
- `fact_store_finance`：门店财务数据，用于门店 TOP、实付交易额、财务口径对账。
- `fact_store_traffic`：门店流量数据，用于曝光、入店、下单、转化率分析。
- `fact_service_review`：评价/售后数据，用于服务指标、用户反馈、P2 文案事实依据。

## 5. 核心代码说明

### `intranet_app/app.py`

系统主入口，负责 HTTP 路由、页面渲染、登录检查、P1-P4 页面、数据入库、P2 内容中心、安踏美团日报/周报生成、插件文件同步。正式报表入口必须从基础层读取数据，缺数据时不能扫描 Downloads 或原始文件兜底。

### `intranet_app/data_foundation.py`

数据入库规则核心，包含上传元信息、字段映射、文件识别、品牌归属评分、校验结果和入库计划。`build_ingestion_plan()` 负责把原始表头和行数据转换成可入库计划，`normalize_rows()` 负责统一字段口径，`validate_standard_values()` 负责校验金额、整数、日期等标准值。

### `intranet_app/storage.py`

本地 SQLite 存储层，负责用户、会话、任务、执行记录、业务反馈、入库批次、源文件、字段映射、校验报告、缺失数据记录和基础事实表。`save_foundation_fact_rows()` 只允许校验通过且 `auto_pass` 的数据写入基础表；`load_meituan_foundation_rows()` 是 P1/P2 读取美团基础数据的统一入口。

### `intranet_app/processors/anta_meituan_reporting.py`

安踏美团日报/周报处理器。日报生成指定日期销售快报、近 7 天 TOP 门店、近 7 天 TOP 商品、流量/服务提示；周报生成周销售汇总、TOP 门店、TOP 商品、下周选品推荐和文案方向。金额计算使用 `Decimal`，缺字段或缺数据时抛出 `ValidationError`。

### `intranet_app/content_pipeline.py`

P2 内容生产流水线。根据日期、品牌、渠道和任务类型选择商品候选，汇总商品销售、销量、订单、评价样例，生成 AI Prompt，解析 AI JSON 返回，输出选品理由、人群场景、卖点、文案、视觉 Brief 和质检提示。

### `intranet_app/ai_gateway.py`

AI 接口网关。负责读取和校验百炼 API Key，调用兼容 OpenAI 格式的百炼接口，处理 HTTP 403、网络失败、超时、空响应、格式错误。未配置 API Key 时禁止用模板文案冒充 AI 生成。

### `intranet_app/scenarios.py`

业务场景注册中心，当前包含 P1 博西短彩信、P1 安踏日报/周报/月报、P2 AI 选品辅助、P2 文案内容辅助、P3 安踏即时零售配置自动化。

## 6. 已完成功能

- 本地内网工作台基础页面。
- 登录、会话、本地 SQLite 存储。
- 首页数据看板与 P1-P4 分级入口。
- 项目开发阶段页面和业务反馈字段。
- 数据入库中心：文件识别、字段映射、校验、品牌归属判断、基础表写入。
- 美团安踏儿童数据基础层：商品订单、门店财务、门店流量、服务评价。
- 安踏美团日报生成，支持日期选择。
- 安踏美团周报生成，支持周维度 TOP 和下周选品方向。
- P2 内容生产中心页面已放在 P2 二级页。
- P2 选品、文案、卖点、视觉 Brief 的第一阶段流水线。
- 百炼 API Key 配置和连接测试逻辑。
- 美团浏览器下载助手与本地同步脚本。
- 自动化任务页面和执行记录。
- 安踏即时零售上下架、素材、黑名单处理逻辑迁移入口。
- GitHub 上传范围清理：真实业务数据、本地数据库、运行产物、大文件已被 `.gitignore` 保护。
- 单元测试覆盖数据基础层、报表、P2 内容、AI 网关、自动化、页面布局等模块。

## 7. 待开发功能

### P1 数据处理

- 完成安踏月报从基础数据层读取的正式版本。
- 增加日报、周报、月报的页面化预览样式，而不只是 CSV 结果。
- 扩展京东、天猫、小程序、官网、CRM 渠道的字段映射。
- 建立跨渠道统一指标口径，例如销售额、订单数、退款、曝光、转化、库存、会员等。

### P2 内容生产

- 将品牌调性、品牌规范、禁用词、产品卖点模板接入正式 P2 流程。
- 增加项目/品牌/渠道下拉选择，支持多品牌共用一套 P2 能力。
- 接入图片生成或图片 Brief 到设计工具的正式交付路径。
- 建立 AI 输出质检页面，支持业务确认、驳回和历史版本沉淀。
- 增加 RAG/知识库检索能力，让 AI 基于品牌资料和商品库生成内容。

### P3 配置自动化

- 把安踏上下架、素材筛选、黑名单筛选做成统一页面化流程。
- 增加配置结果复核、导出、历史记录和异常案例归档。
- 扩展其他品牌和平台后台的配置类规则。

### 自动化与插件

- 完善美团插件从后台导出到基础层入库的端到端稳定性。
- 增加“缺数据自动提示业务导出”的任务状态。
- 明确每个渠道的非 API 自动化方式：浏览器插件、固定导出模板、人工上传、OCR/页面采集。
- 增加任务调度策略，支持每日、每周、每月定时提醒或半自动执行。

### 部署与协作

- 在公司信息安全允许的前提下，确定内网部署方式。
- 增加多用户权限、角色、操作日志、审计记录。
- 准备信息安全评审材料，包括数据不出域、密钥不入库、账号不共享、操作留痕。

## 8. 技术债务

- 当前工作台仍是轻量本地 Web 服务，未达到公司级生产部署要求。
- SQLite 适合本地试运行，多用户并发和长期数据治理需要升级到受控数据库。
- 部分页面 HTML 仍集中在 `app.py`，后续需要拆分模板或前端组件，降低维护成本。
- P1/P2 的多渠道数据模型尚未完全统一，目前美团链路最成熟。
- P2 AI 输出依赖外部模型权限和额度，HTTP 403 等问题需要在配置页给出更清晰诊断。
- 浏览器插件依赖用户已登录后台，无法绕过验证码、MFA、权限和平台风控。
- 品牌资料、商品库、活动日历、禁用词库还需要标准化录入和版本管理。
- 页面权限目前偏轻量，后续共享给业务或开发者前需要补齐角色权限和操作审计。
- 自动化任务当前更偏半自动，需要继续强化失败重试、缺失数据提示、导出状态识别。
- 真实业务数据仍在本地目录中，必须持续依赖 `.gitignore` 和人工复核，避免误上传。

## 附：开发约束

- 正式 P1/P2 输出必须读取统一基础数据层。
- 原始 Excel/CSV 只能作为 intake 输入或诊断材料，不能直接作为正式报表/AI 生成源。
- 金额计算使用 `Decimal`。
- 缺少基础层数据时必须失败闭环，提示补数，不能静默降级。
- API Key、账号密码、Cookie、真实业务数据禁止写入 Git。
- 浏览器插件只允许浏览、下载、同步，不允许对业务后台做配置修改。
